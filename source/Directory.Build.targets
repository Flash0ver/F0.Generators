<Project>

  <PropertyGroup Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net5.0'))">
    <DefineConstants>$(DefineConstants);HAS_GENERIC_ENUM_GETNAME</DefineConstants>
  </PropertyGroup>

</Project>
